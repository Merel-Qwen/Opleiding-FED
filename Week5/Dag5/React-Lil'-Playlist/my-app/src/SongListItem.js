import React from "react";

const SongListItem = () => {
  return (
    <li className="list-item">
      <span> Song item</span>
    </li>
  );
  //className="list-item" key={song.id} value={songy.title}
};

export default SongListItem;
